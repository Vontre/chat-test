This chat service is an EXTREMELY BASIC DEMO, and would not function well in a real environment.

It does not handle message ordering, disconnects, error handling, message redundancy; it only works reliably in a localhost demo environment.